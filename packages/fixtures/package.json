{
  "name": "@typepoint/fixtures",
  "version": "4.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index",
  "license": "ISC",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "yarn clean && tsc -p ./tsconfig.main.json --noEmit false",
    "lint": "yarn eslint -c '../../.eslintrc.js' ./src/**/* --ext .js,.jsx,.ts,.tsx --format visualstudio",
    "validate": "yarn lint && yarn build"
  }
}
